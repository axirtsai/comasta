# Preserved versions

`versions/v0.1/` contains the repository's v0.1-draft source materials as they existed at commit `23a878a` before the root specification was migrated to v0.2-draft.

Historical experiment outputs remain in `tests/results/`. They were not rewritten during the v0.2-draft migration and are not evidence that v0.2 improves underlying language-model reasoning.
